---Models in module 'google_calendar'---
obsolete model google.calendar.sync [abstract]
new model google.event.sync [abstract]
new model google.sync [abstract]
# NOTHING TO DO: an abstract model has no table
---Fields in module 'google_calendar'---
google_calendar / calendar.calendar        / google_id (char)              : NEW
google_calendar / calendar.calendar        / is_import_pending (boolean)   : NEW
# NOTHING TO DO: empty is the right value for existing records
google_calendar / calendar.calendar        / need_sync (boolean)           : NEW hasdefault: default
# NOTHING TO DO: the default is True, so both calendars came out flagged as
# needing a sync rather than NULL -- and that is the safe direction. A record
# wrongly marked as needing a sync is re-sent; one wrongly marked as synced is
# never sent again
google_calendar / calendar.event           / last_google_calendar_sync_path (char): NEW
google_calendar / calendar.recurrence      / last_google_calendar_sync_path (char): NEW
# NOTHING TO DO: empty is the right value for existing records
google_calendar / calendar.user            / access_role (False)           : NEW selection_keys: ['freeBusyReader', 'owner', 'reader', 'writer'], mode: modify
# NOTHING TO DO: keys were added to the selection, the existing values stay valid
google_calendar / calendar.user            / google_sync_enabled (boolean) : NEW hasdefault: compute
# NOTHING TO DO: a stored compute, so the ORM fills it -- both calendar.user
# rows came out carrying the computed value rather than a NULL
google_calendar / calendar.user            / google_sync_token (char)      : NEW
google_calendar / res.users.settings       / google_account_email (char)   : NEW
# NOTHING TO DO: empty is the right value for existing records
google_calendar / res.users.settings       / google_calendar_cal_id (char) : DEL
# NOTHING TO DO: the column is left in place for database_cleanup
---XML records in module 'google_calendar'---
NEW ir.access: google_calendar.google_calendar_account_reset
DEL ir.model.access: google_calendar.google_calendar_account_reset
# NOTHING TO DO: base folds ir.model.access and ir.rule into ir.access, keeping the xml_ids
NEW ir.ui.view: google_calendar.calendar_calendar_view_form_inherit_google_calendar
NEW ir.ui.view: google_calendar.calendar_calendar_view_list_inherit_google_calendar
NEW ir.ui.view: google_calendar.calendar_user_view_form_inherit_google_calendar
# NOTHING TO DO: the record is not noupdate, so the module update creates it
