---Models in module 'pos_loyalty'---
---Fields in module 'pos_loyalty'---
pos_loyalty  / pos.order                / active_payment_programs (json): NEW hasdefault: default
pos_loyalty  / pos.order                / active_rewards (json)         : NEW hasdefault: default
pos_loyalty  / pos.order                / applied_codes (json)          : NEW hasdefault: default
pos_loyalty  / pos.order                / disabled_program_ids (json)   : NEW hasdefault: default
# NOTHING TO DO: a new field whose default is falsy -- False, or an empty
# list -- so a NULL left on an existing row already means what the default
# would have written. The one flagged field whose default names a record,
# pos.config.default_product_id, has a script instead
pos_loyalty  / pos.order.line           / card_id (many2one)            : NEW relation: loyalty.card
# NOTHING TO DO: empty is the right value for existing records
pos_loyalty  / pos.order.line           / coupon_id (many2one)          : DEL relation: loyalty.card
# NOTHING TO DO: the column is left in place for database_cleanup
pos_loyalty  / pos.order.line           / gift_card_vals (json)         : NEW
# NOTHING TO DO: empty is the right value for existing records
pos_loyalty  / pos.order.line           / reward_identifier_code (char) : DEL
# NOTHING TO DO: the column is left in place for database_cleanup
---XML records in module 'pos_loyalty'---
barcode.rule: pos_loyalty.barcode_rule_coupon (noupdate switched)
# NOTHING TO DO: only the noupdate flag changed, the definition is the same
NEW ir.access: pos_loyalty.access_applicability_pos_manager
NEW ir.access: pos_loyalty.access_applicability_pos_user
NEW ir.access: pos_loyalty.access_communication_pos_manager
NEW ir.access: pos_loyalty.access_communication_pos_user
NEW ir.access: pos_loyalty.access_coupon_pos_manager
NEW ir.access: pos_loyalty.access_coupon_pos_user
NEW ir.access: pos_loyalty.access_loyalty_card_update_balance_pos_user
NEW ir.access: pos_loyalty.access_loyalty_history_pos_user
NEW ir.access: pos_loyalty.access_program_pos_manager
NEW ir.access: pos_loyalty.access_program_pos_user
NEW ir.access: pos_loyalty.access_reward_pos_manager
NEW ir.access: pos_loyalty.access_reward_pos_user
NEW ir.access: pos_loyalty.access_sale_coupon_generate
DEL ir.model.access: pos_loyalty.access_applicability_pos_manager
DEL ir.model.access: pos_loyalty.access_applicability_pos_user
DEL ir.model.access: pos_loyalty.access_communication_pos_manager
DEL ir.model.access: pos_loyalty.access_communication_pos_user
DEL ir.model.access: pos_loyalty.access_coupon_pos_manager
DEL ir.model.access: pos_loyalty.access_coupon_pos_user
DEL ir.model.access: pos_loyalty.access_loyalty_card_update_balance_pos_user
DEL ir.model.access: pos_loyalty.access_loyalty_history_pos_user
DEL ir.model.access: pos_loyalty.access_program_pos_manager
DEL ir.model.access: pos_loyalty.access_program_pos_user
DEL ir.model.access: pos_loyalty.access_reward_pos_manager
DEL ir.model.access: pos_loyalty.access_reward_pos_user
DEL ir.model.access: pos_loyalty.access_sale_coupon_generate
# NOTHING TO DO: base folds ir.model.access and ir.rule into ir.access, keeping the xml_ids
NEW ir.ui.view: pos_loyalty.pos_order_receipt
# NOTHING TO DO: the record is not noupdate, so the module update creates it
product.product: loyalty.ewallet_product_50 (noupdate switched)
product.product: loyalty.gift_card_product_50 (noupdate switched)
# NOTHING TO DO: only the noupdate flag changed, the definition is the same
