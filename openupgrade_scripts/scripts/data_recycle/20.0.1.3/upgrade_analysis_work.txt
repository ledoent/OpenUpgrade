---Models in module 'data_recycle'---
---Fields in module 'data_recycle'---
data_recycle / data_recycle.record      / res_id (integer)              : relation is now 'res_model_name' ('False') [nothing to do]
data_recycle / data_recycle.record      / res_id (integer)              : type is now 'many2one_reference' ('integer')
# NOTHING TO DO: the ORM converts the column in place. Checked across the
# upgrade: no field flagged here comes out holding fewer values than it did
---XML records in module 'data_recycle'---
NEW ir.access: data_recycle.access_data_recycle_model_group_system
NEW ir.access: data_recycle.access_data_recycle_record_group_system
# NOTHING TO DO: base folds ir.model.access and ir.rule into ir.access, keeping the xml_ids
ir.actions.act_window: data_recycle.action_data_recycle_config (deleted domain)
ir.actions.act_window: data_recycle.action_data_recycle_record (deleted domain)
# NOTHING TO DO: the record is not noupdate, so the module update rewrites the domain
DEL ir.model.access: data_recycle.access_data_recycle_model_group_system
DEL ir.model.access: data_recycle.access_data_recycle_record_group_system
# NOTHING TO DO: base folds ir.model.access and ir.rule into ir.access, keeping the xml_ids
ir.model.constraint: data_recycle.constraint_data_recycle_model_check_notif_freq (changed definition: is now 'False' ('CHECK(notify_frequency > 0)'))
# NOTHING TO DO: the record is not noupdate, so the module update rewrites it
