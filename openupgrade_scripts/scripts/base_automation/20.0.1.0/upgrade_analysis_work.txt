---Models in module 'base_automation'---
---Fields in module 'base_automation'---
base_automation / base.automation          / previous_domain (char)        : now a function
# NOTHING TO DO: the field is stored in 20.0 and filled by its own compute.
# Every field the analysis flags that is stored and entirely empty afterwards
# was checked one by one; the only pair whose emptiness meant a lost value was
# stock_delivery's hs_code and country_of_origin, which now have a script
base_automation / base.automation          / website_message_ids (one2many): DEL relation: mail.message
# NOTHING TO DO: a one2many has no column, so nothing is left behind
---XML records in module 'base_automation'---
NEW ir.access: base_automation.access_base_automation_config
# NOTHING TO DO: base folds ir.model.access and ir.rule into ir.access, keeping the xml_ids
ir.cron: base_automation.ir_cron_data_base_automation_check (noupdate switched)
# NOTHING TO DO: only the noupdate flag changed, the definition is the same
DEL ir.model.access: base_automation.access_base_automation_config
# NOTHING TO DO: base folds ir.model.access and ir.rule into ir.access, keeping the xml_ids
